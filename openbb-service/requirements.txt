# Pinned to a specific minor version so the openbb-api CLI entry point is stable.
# If pip rejects this exact version, check https://pypi.org/project/openbb/#history
# and update to the nearest available 4.x release.
openbb==4.2.5
openbb-fmp>=0.1.0
httpx>=0.27.0,<1.0.0
uvicorn[standard]>=0.29.0
